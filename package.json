{
  "name": "venom-bot-whatsapp-webhook",
  "version": "1.1.0",
  "description": "A simple webhook server for Venom Bot to handle messages request via API.",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run ./index.ts",
    "start:node": "ts-node ./index.ts"
  },
  "dependencies": {
    "@types/express": "^5.0.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "venom-bot": "^5.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.15.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
